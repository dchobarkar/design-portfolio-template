const ServicesListCSS = {
  root: {
    minHeight: "var(--minHeight)",
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "center",
    alignItems: "center",
  },
};

export default ServicesListCSS;
